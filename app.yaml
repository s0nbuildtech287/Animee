runtime: python311
entrypoint: gunicorn -b :$PORT app:app

automatic_scaling:
  max_instances: 10
  min_instances: 1
  target_cpu_utilization: 0.65

handlers:
  - url: /static
    static_dir: static
  - url: /.*
    script: auto

env_variables:
  CLOUD_SQL_CONNECTION_NAME: "anime-library-461214:asia-east1:anime-library-db"
  CLOUD_SQL_USERNAME: "root"
  CLOUD_SQL_PASSWORD: "123456"
  CLOUD_SQL_DATABASE_NAME: "anime-db"
